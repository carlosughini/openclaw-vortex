# Session Notes — 2026-04-13

## Critical Security Response (Active Attack in Progress)

### Incident Summary
- **Active SSH brute-force attack** from IP `171.244.135.35`
- **Attack pattern:** User "admin1" brute-force on multiple ports (28087, 36038, 16664, 32565, 59649, etc.)
- **Additional attacker:** `101.204.251.227` attempted with "admin1" on port 18956
- **Timeline:** ~75 minutes of repeated failed login attempts

### Security Actions Completed

#### 1. Traefik Restarted
- Container: `traefik.1.bhkqzvd4sxgonn7bfgsrkb67y`
- Reason: Possible compromise or stale state

#### 2. UFW Firewall Configured
- Status: Active and enabled
- Rules configured:
  ```
  Default: DENY incoming
  22/tcp    ALLOW (SSH - consider changing to non-standard port)
  80/tcp    ALLOW (HTTP)
  443/tcp   ALLOW (HTTPS)
  18789     ALLOW (OpenClaw gateway - internal)
  148.230.73.251/32 ALLOW (Cloudflare/Tailscale - any port)
  ```

#### 3. Fail2Ban Enabled & Running
- Status: Active
- Jail: sshd
- Configuration:
  ```
  enabled = true
  port = ssh
  filter = sshd
  logpath = /var/log/auth.log
  maxretry = 5
  bantime = 3600 (1 hour)
  findtime = 600
  ```
- Current stats:
  - Currently banned: 0
  - Total banned: 2 (attacker IPs already blocked)
  - Total failed: 95

#### 4. Attack IPs in Fail2Ban
- `171.244.135.35` — Primary brute-force attacker (logged multiple times)
- `101.204.251.227` — Secondary attacker

### Immediate Recommendations

#### CRITICAL: Change SSH Port
- **Current:** Port 22 (exposed to entire internet)
- **Action:** Change SSH to non-standard port (e.g., 2222, 2223, etc.)
- **Why:** 22 is the default port attackers scan first
- **Steps:**
  1. Edit `/etc/ssh/sshd_config` → change `Port 22` to `Port 2222`
  2. Update UFW: allow 2222/tcp instead of 22/tcp
  3. Restart sshd: `systemctl restart sshd`

#### HIGH: Consider Key-Only Authentication
- Disable password authentication (requires SSH keys only)
- Edits `/etc/ssh/sshd_config`:
  ```bash
  PasswordAuthentication no
  PubkeyAuthentication yes
  ```
- This eliminates password-based brute-force attacks entirely

#### MEDIUM: Monitor Server Load
- The brute-force is consuming resources
- Check high-CPU processes: `top` or `htop`

### Mission Control Status
- **Removed:** All containers stopped, Traefik routing cleaned up
- **Repository:** `carlosughini/personal-os-mission-control-1/tree/design-colorful` (correct branch)
- **Backend:** Node 22 + Express + SQLite (Debian-based)
- **Frontend:** React + Vite + Tailwind

### Next Steps for Mission Control

When ready to re-deploy:
1. Choose deployment approach:
   - A) **Caddy reverse proxy** (simpler, handles HTTPS automatically)
   - B) **Nginx reverse proxy** (common, well-documented)
   - C) **Direct Docker network** with Traefik on bridge network
   - D) **Port 3002/3001/3003** directly through firewall (simpler, no proxy needed)

2. **Consider production build** instead of dev server:
   - Build static assets with `npm run build`
   - Serve with nginx or Caddy
   - Faster and more secure than Vite dev server in production

3. **SSL/TLS:**
   - For Caddy: automatic Let's Encrypt
   - For Nginx: use Certbot
   - Or use existing Cloudflare SSL certificates

### Server Security Checklist
- [x] UFW enabled and configured
- [x] Fail2Ban enabled and running
- [ ] SSH port changed from 22 to non-standard (e.g., 2222)
- [ ] Password authentication disabled (key-only auth)
- [ ] Server load monitored
- [ ] Unnecessary ports closed (3000, 4000, 5173, 1234 - close if not needed)
