# Session Notes — 2026-04-13 (Supplement)

## Service Binding Fixes in Progress

### Issues Identified
1. **LiteLLM service** — Running on `0.0.0.0:4000` (public interface)
   - Should bind to `127.0.0.1` only
   - Running as direct process (no systemd service)
   - Requires restart to apply binding changes

2. **Docker Proxy** — Running with two instances
   - Correctly binding to `127.0.0.1` (private)
   - Already properly configured
   - Service was killed for restart but needs proper restart method

3. **Other services on 0.0.0.0:**
   - LiteLLM (4000) — needs binding fix
   - Potentially others

### Actions Taken
1. Created `/root/fix-bindings.sh` script to check port conflicts
2. Attempted to stop docker-proxy (failed - service not managed by systemd)
3. Attempted to restart litellm (no systemd service)

### Next Actions Required
1. **Restart LiteLLM with proper binding:**
   ```bash
   # Kill existing process
   pkill -9 litellm
   # Restart with bind flag
   /usr/bin/litellm --bind 127.0.0.1 --api_base https://open.bigmodel.cn/api/paas/v4 &
   ```

2. **Or configure systemd service for LiteLLM** (if one exists) with:
   - `Bind=127.0.0.1`
   - Or `Environment="BIND=127.0.0.1"`

3. **Verify after restart:**
   ```bash
   lsof -i :4000 -P -n | grep LISTEN
   # Should show: TCP *:4000 (LISTEN) bound to 127.0.0.1
   ```

4. **Consider Docker Compose for these services** to add proper port bindings

### Status
- UFW: ✅ Active and configured
- Fail2Ban: ✅ Running
- LiteLLM: ⏳ Awaiting proper restart with binding fix
- Docker Proxy: ⏳ Needs proper restart after changes

### Not Yet Done
- Docker compose file fixes (need to find where these services are defined)
- Notion token moved to env var
- Services still exposing on 0.0.0.0 pending verification
